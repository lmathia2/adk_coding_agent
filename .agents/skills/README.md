# Google Agents CLI skills

Run `python scripts/sync_agents_cli_skills.py` to copy the selected, pinned upstream skills into this directory. The generated skill directories are intentionally ignored by neither Git nor the script; teams may choose to commit the synchronized copies for hermetic development.

The source revision and selected skills are recorded in `upstream-lock.json`.
